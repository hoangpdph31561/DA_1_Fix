@page "/"

@code {
    [Inject]
    NavigationManager navigation { get; set; }
    protected override async Task OnInitializedAsync()
    {
        navigation.NavigateTo("/customer/RoomType",forceLoad: true); // chuyển hướng đến trang login đầu tiên
    }
}
